import { BlockContext, IMarkdown, IPlainText, TextObject } from '../blocks';
import { IParser } from '../parsers/IParser';
import { ElementRenderer } from '../renderers/ElementRenderer';
export declare const uiKitText: (parser: IParser<unknown>, conditions?: import("../blocks").Conditions) => (blocks: unknown) => unknown;
export declare abstract class UiKitParserText implements IParser<unknown> {
    plainText: ElementRenderer<unknown, IPlainText>;
    mrkdwn: ElementRenderer<unknown, IMarkdown>;
    text: (text: TextObject, context: BlockContext, index: number) => unknown;
}
